<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FTS" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FTS" parent="Base.Theme.FTS" />
    <style name="Theme.Card.FTS" parent="Base.Theme.FTS" />
    <style name="ShapeAppearanceOverlay.App.Card.Media" parent="">
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
        <item name="cornerSizeTopLeft">8dp</item>
        <item name="cornerSizeTopRight">8dp</item>
    </style>

    <style name="Theme.Card.FTS.ActionBar" parent="Theme.Material3.DayNight">
        <item name="fontFamily">@font/alata</item>
        <item name="colorPrimary">#000</item>
        <item name="android:statusBarColor">#fff</item>
        <item name="android:windowBackground">#fff</item>
    </style>
</resources>